namespace CustomCompiler
{
    public enum TokenType
    {
        Plus = '+',
        Minus = '-',
        Multiplication = '*',
        Division = '/',
        LParen = '(',
        RParen = ')',
        EOF = (char)0,
        Digit = (char)1
    }
}
